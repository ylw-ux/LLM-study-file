
==前置学习如果了解了基本的transformers内部的数据流态，会更有利于理解
一共有7集,包括深度学习 attention Transformer
【【官方双语】深度学习之神经网络的结构 Part 1 ver 2.0】 https://www.bilibili.com/video/BV1bx411M7Zx/?share_source=copy_web&vd_source=5936b93e69fb906e9fb1ca0683685589==

学会在本地部署自己的大模型（比如 Qwen2.5-1.5B-Instruct）进行推理
1.使用 Transformer 包直接进行部署推理；

### 一 下载本地大模型包（较大，好多G）
这里采用 Qwen2.5-1.5B-Instruct 

这里采用国内的镜像modelscope
**网址：https://www.modelscope.cn/my/overview**

大模型的权重文件通常非常大（哪怕是 Qwen2.5-1.5B 也有将近 3GB）。纯小白最容易犯的错误就是**用代码自动下载，结果根本不知道模型文件被下载到了系统盘的哪个隐藏角落**。

![[Pasted image 20260528180122.png]]


![[Pasted image 20260528180157.png]]
我们要做到“显式管理”，把模型下载到我们指定的路径。

1. 在你的项目文件夹 `my_llm_project` 下，新建一个名为 `models` 的文件夹。
    
2. 我们要下载的目标模型是 **`Qwen2.5-1.5B-Instruct`**（通义千问 15 亿参数的指令微调版本，非常适合新手在笔记本上跑）。
![[Pasted image 20260528175545.png]]
我知道这很难，所以有好办法，让ai生成一个下载指令(⊙o⊙)
### 第一步：在终端安装魔搭工具包

确保你的 VS Code 终端最前面依然有绿色的 **`(llm_env)`**，然后输入以下命令并回车：

```shell
pip install modelscope
```
### 第二步：写一个下载脚本
在你的项目根目录下，新建一个名为      download_model.py     的 Python 文件，把下面这段代码复制进去：
``` python
import os
from modelscope import snapshot_download

# 1. 明确指定你希望模型下载到本地的哪个路径
# 这里我们把它定在项目下的 models 文件夹中
local_dir_path = "./models/Qwen2.5-1.5B-Instruct"

print("🚀 开始下载 Qwen2.5-1.5B-Instruct 模型...")
print("📦 模型文件较大（约3GB），请保持网络畅通，耐心等待...\n")

# 2. 呼叫魔搭的下载函数
model_dir = snapshot_download(
    'Qwen/Qwen2.5-1.5B-Instruct', # 魔搭网上的模型ID
    local_dir=local_dir_path      
    # 强行指定下载到你刚设定的本地目录,由于项目根目录下新建这个文件，
    # 这里推荐local_dir_path 为 ./model.py 
    #（点就是返回当前文件的上一级，也就是项目根目录，他下面的download_model.py）
)

print(f"\n🎉 下载成功！模型已完整保存在: {model_dir}")

```
### 第三步：运行下载

在终端中输入并回车：
``` shell
python download_model.py
```
然后你就会看到根目录下面有一个文件夹model，model里有一个Qwen2.5-1.5B-Instruct文件夹，就是本地大模型文件

### 二 运行本地大模型

### 环境问题
### 关于 PyTorch (`torch`) 的重要说明

`transformers` 只是一个上层外壳，它的底层运算结结实实地依赖 **PyTorch**。
你的电脑是拥有强劲独立显卡的比如英伟达50X系列。如果想让模型运行得像飞一样快，就必须让 Python 能够调用你的 NVIDIA 显卡（CUDA 运算），而不是用 CPU 死扛。
这个到后面的微调训练也一定会用到CUDA
请在终端里输入以下命令，检查你当前环境里到底有没有安装 `torch`，以及它能不能识别到你的显卡：
``` shell

python -c "import torch; print('Torch版本:', torch.__version__); print('显卡是否可用:', torch.cuda.is_available())"
```

### 运行后请观察结果：

- **情况 A：如果直接报错说没有 `torch`，或者显示“显卡是否可用: False”** 说明你目前环境里只有 CPU 版本的 torch，或者干脆没装。
- **情况 B：如果显示“显卡是否可用: True”** 那太棒了！说明你的虚拟环境已经具备了显卡加速能力。
- 在终端执行以下命令，安装带 最新版CUDA 加速的官方 PyTorch（**注意：这个库大约有 2.6GB，下载需要一点时间**）：
- 这个是pytouch的官方网址,进去就可以直接看到版本。
- 根据你的系统选择：https://pytorch.org/get-started/locally/
- 这个是2026年5月截止最新的，根据实际情况来![[Pasted image 20260528183102.png]]
在你的终端运行这段代码
``` shell

pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu132

```
安装完之后再输入这个验证是否安装完成
``` shell

pip show transformers accelerate torch 

```
![[Pasted image 20260528183803.png]]



### 这是完整的代码运行
``` python
# 导入 transformers 的核心组件：自动分词器和因果语言模型类
from transformers import AutoTokenizer, AutoModelForCausalLM

MODEL_PATH = "./models/Qwen2.5-1.5B-Instruct" #这里要填你自己的真实地址哦:)
print("正在通过 transformers 加载本地模型和分词器...")
# [学以致用]: 这里的 .from_pretrained 是面向对象里典型的“类方法”
# 它会去 MODEL_PATH 路径下自动寻找并解析 config.json、vocab.json 等文件
# 加载分词器对象（负责 Encode 和 Decode）
tokenizer = AutoTokenizer.from_pretrained(MODEL_PATH)
# 加载大模型对象（负责矩阵计算生成）
# device_map="auto" 会让 transformers 自动侦测你的硬件：有 NVIDIA 显卡就上显卡，没有就用 CPU
model = AutoModelForCausalLM.from_pretrained(
    MODEL_PATH,  
    device_map="auto",
    torch_dtype="auto"  # 自动匹配最适合你显卡/CPU 的数据精度（如 float16）
)
print("\n🎉
本地大模型加载成功！你可以开始和它对话了。")
print("输入 'quit' 退出。")
while True:
    user_input = input("\n👤 我: ").strip()
    if not user_input or user_input.lower() == 'quit':
        break
    messages = [
        {"role": "system", "content": "你是一个运行在本地的AI助理。"},
        {"role": "user", "content": user_input}
    ]
    # 【步骤 A】: 套用模型的对话模板
    # 这步会把字典列表加上特殊符号，拼成一行类似 <|im_start|>user\n... 的连续字符串
    text = tokenizer.apply_chat_template(
        messages,
        tokenize=False,
        add_generation_prompt=True
    )
    # 【步骤 B】: Encode（编码）
    # 把文本转成 PyTorch 的 Tensor（张量数字），并送入对应的计算设备（显卡或CPU）
    model_inputs = tokenizer([text], return_tensors="pt").to(model.device)
    print("🤖 思考中...")
    # 【步骤 C】: Generate（调用模型对象的方法生成预测数字）
    # 这里的 max_new_tokens 限制了自回归循环的最大次数
    # 传入model输出预测的token
    generated_ids = model.generate(
        **model_inputs,   #上下文传入
        #这一堆是超参数，用来控制模型的，有点像F1调零件吧，不懂。。。。
        max_new_tokens=512, #最大输出token
        min_new_tokens = 10,
        temperature=0.7, #温度 随机性，越大输出越多变， 低———理性 高——浪漫
        top_p = 50,  #控制query和key向量取值的范围，
        top_k = 50, #这里是取前50%
        #以下的do_sample = True,num_return_sequences = 5,第一次我可以先不打开
        #和注释掉的print可以同时使用，num_return_sequences = 5, 让model一次返回5个不同的回复，可以这么理解
        #do_sample = True, 
        #num_return_sequences = 5, 
        #采样 如果打开 model不管什么概率的样本都可能选中 会导致每次回答基本不一样
        #开启采样也是实现自我一致性的基础
    )
    # 【步骤 D】: 把模型新吐出来的 Token 截取出来
    # 因为生成的数字里包含了你输入的 Prompt，我们需要把新吐出的字切片出来
    cut_len  = len(model_inputs.input_ids[0])
    #有人可能会疑惑，这里为什么有两个一样的参量generated_ids，因为再重新赋值了，把之前覆盖掉了，好吧:)
    generated_ids = [
        output_ids[cut_len:] for output_ids in  generated_ids
    ]
    # 【步骤 E】: Decode（解码）
    # 把数字重新翻译回人类看懂的汉字字符串，并跳过特殊控制符（skip_special_tokens=True）
    response = tokenizer.batch_decode(generated_ids, skip_special_tokens=True)
    #第一种输出配合采样一起使用，回答更加清晰
    #cnt = 1
    #for rep in response:
       # print(f"🤖 本地模型:第{cnt}条采样是{rep}")
       # cnt+=1
    #第二种普通输出
    print(response)

```
开启采样的形式，一般来讲，归一化处理是为了选取一个最有可能的答案通过采样多条数据，但是还需要复杂的运算，这里就只是把5条采样全部输出了
![[Pasted image 20260528184922.png]]

 来分析一下这个架构它和那个API形式调用有什么区别：
- 首先，API这个外部调用的话，它是要有一个密钥和一个外部的网址 ，这里它要有的就是本地模型的地址，
- 然后都同时要对这个大模型进行一个初始化，就是让它创建一个实例，变成一个实体，它具体是什么类型的，它采用的是谁的Key，他用的是什么模型
- OpenAI调用API的话，可以直接调用对应的方法来对大模型进行一个请求，然后获取他的回复
- Transformer则就复杂得多，首先它要对我们输入的messengers，也就是文本或者上下文，首先要把它给进行一个格式化。然后由tokenier进行分词，然后转换成张量，也就是高维坐标
- 然后我们再把这个高维坐标通过方法调用传给这个本地的大模型，然后它再返回我们的回复，当然这个回复也是一堆坐标，还要对这个坐标进行解码，把它转化为文本
- 这一堆坐标里面是包含了这个我们的问题的，所以需要把它进行截取，只取回复的一部分的Token坐标
- 最后都是进行输出，没毛病！！！
